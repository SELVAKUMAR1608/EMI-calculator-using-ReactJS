import React, { useState } from 'react'

function Tenure() {
  // let[tenure,settenure]=useState(0);

  // let getTenure=(e)=>{
  //   settenure(e.target.value);
  // }
  // console.log(tenure);
  
  return (

    <div>
        {/* <h3>Tenure</h3>
        <h3>{tenure}</h3>
        <input type="range" min={12} max={48} step={1} value={tenure} onChange={getTenure}/> */}
    </div>
  )
}

export default Tenure